(executables
 ((names (example))
  (libraries (virtual_dom
              core_kernel
              async_kernel
              async_js_extended
              js_of_ocaml))
  (preprocess (pps (js_of_ocaml.ppx.driver ppx_jane)))
  ))

(jbuild_version jane_street)

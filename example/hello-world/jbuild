(executables
 ((names (example))
  (libraries (virtual_dom
              js_of_ocaml_async
              core_kernel
              async_kernel
              js_of_ocaml))
  (js_of_ocaml ((flags (--pretty
                       --disable shortvar
                       --enable debuginfo
                       --source-map
                       ))))
  (preprocess (((pps (ppx_js_driver JANE_KERNEL)) All)))
  ))

(executables
 ((names (terminal))
  (libraries (virtual_dom
              core_kernel
              js_of_ocaml))
  (preprocess (pps (js_of_ocaml-ppx  ppx_jane ppxlib.runner)))
  ))


(jbuild_version 1)

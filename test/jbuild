(library (
  (name virtual_dom_test)
  (preprocess (pps (js_of_ocaml.ppx.driver ppx_jane)))
  (inline_tests ((javascript build_and_run) (native dont_build_dont_run)))
  (js_of_ocaml ())
  (libraries (
    core_kernel
    js_of_ocaml
    virtual_dom
  ))
))

(jbuild_version jane_street)

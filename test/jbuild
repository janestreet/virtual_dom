(library (
  (name virtual_dom_test)
  (preprocess (pps (js_of_ocaml.ppx.driver ppx_jane ppx_driver.runner)))
  (libraries (
    core_kernel
    js_of_ocaml
    virtual_dom
  ))
))

(jbuild_version 1)

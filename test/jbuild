(library (
  (name virtual_dom_test)
  (preprocess (((pps (ppx_js_driver JANE_KERNEL)) All)))
  (inline_tests ((javascript build_and_run) (native dont_build_dont_run)))
  (js_of_ocaml ((flags (../lib/virtualdom.compiled.js))))
  (libraries (
    core_kernel
    js_of_ocaml
    virtual_dom
  ))
))

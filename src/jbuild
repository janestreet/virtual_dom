(library (
  (name virtual_dom)
  (public_name virtual_dom)
  (preprocess (pps (js_of_ocaml.ppx.driver ppx_jane ppx_driver.runner)))
  (libraries (
    tyxml.functor
    js_of_ocaml
    base
  ))
))

(jbuild_version 1)

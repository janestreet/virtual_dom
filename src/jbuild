(library (
  (name virtual_dom)
  (public_name virtual_dom)
  (inline_tests ((native dont_build_dont_run)))
  (preprocess (pps (js_of_ocaml.ppx.driver ppx_jane.kernel)))
  (js_of_ocaml ((javascript_files (../lib/virtualdom.compiled.js ./soft-set-hook.js))))
  (libraries (
    tyxml
    js_of_ocaml
    core_kernel
  ))
))
